<%= form_with(model: bicyclecategory, local: true) do |form| %>
  <% if bicyclecategory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bicyclecategory.errors.count, "error") %> prohibited this bicyclecategory from being saved:</h2>

      <ul>
      <% bicyclecategory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :bicyclecategory_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
